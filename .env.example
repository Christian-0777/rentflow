# RentFlow Environment Configuration Guide
# =========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive credentials

# =========================================
# DATABASE CONFIGURATION
# =========================================
# Database host (usually localhost for local development)
DB_HOST=localhost

# Database name - the MySQL database you created for RentFlow
DB_NAME=rentflow

# Database username - MySQL user with access to the database
DB_USER=root

# Database password - leave empty for local development if no password is set
DB_PASS=

# =========================================
# EMAIL / SMTP CONFIGURATION
# =========================================
# SMTP server host (e.g., smtp.gmail.com, smtp.mailtrap.io, etc.)
MAIL_HOST=smtp.gmail.com

# SMTP port (587 for TLS, 465 for SSL)
MAIL_PORT=587

# SMTP username / email address
# For Gmail: use your Gmail address
# For other services: check their documentation
MAIL_USERNAME=your-email@gmail.com

# SMTP password or app password
# For Gmail with 2FA enabled: generate an App Password from your Google Account settings
# For Gmail without 2FA: use your Gmail password
# For other services: check their documentation
MAIL_PASSWORD=your-app-password-here

# Sender email address (can be different from MAIL_USERNAME)
# This is the "From" address that appears in emails sent by RentFlow
MAIL_FROM=no-reply@rentflow.local

# Sender name (appears as the sender name in emails)
MAIL_FROM_NAME=Rentflow Team

# =========================================
# APPLICATION SETTINGS
# =========================================
# Environment: development, testing, production
APP_ENV=development

# Debug mode (true for development, false for production)
# Set to true to show detailed error messages (development only!)
# Set to false in production to hide sensitive information
APP_DEBUG=true

# Application name
APP_NAME=RentFlow

# =========================================
# PENALTY CONFIGURATION
# =========================================
# Daily penalty rate on amount due (as a decimal)
# 0.02 = 2% daily penalty
# Example: If rent is $1000, daily penalty is $20
PENALTY_RATE=0.02

# =========================================
# SETUP INSTRUCTIONS
# =========================================
# 1. Copy this file: cp .env.example .env
# 2. Update the values in .env with your actual configuration
# 3. For Gmail SMTP:
#    - Enable "Less secure app access" OR
#    - Generate an App Password if 2FA is enabled
# 4. Test database connection by accessing the admin dashboard
# 5. Test email by triggering a password reset email
# =========================================
